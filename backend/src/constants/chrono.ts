/**
 * This is the interval in minutes, denoting how long after a course is created it can be deleted.
 * After this time, the course can't be deleted anymore
 * (it will live in the system forever because the requirement said so).
 */
export const DELETE_INTERVAL_IN_MINUTES = 30;
